bot:
  token: "YOUR TELEGRAM BOT TOKEN"
parser:
  rules:
    - name: "errors"
      pattern: ".*ERROR.*"
    - name: "warnings"
      pattern: ".*WARN.*"
logs:
  path: "logs.log"
database:
  path: "bot.db"
batch:
  size: 1000
  interval: 5s
  policy: "drop_oldest"
