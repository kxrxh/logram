bot:
  token: "YOUR TELEGRAM BOT TOKEN"
parser:
  rules:
    - name: "errors"
      pattern: ".*ERROR.*"
    - name: "warnings"
      pattern: ".*WARN.*"
logs:
  path: "bot.log"
database:
  path: "bot.db"
